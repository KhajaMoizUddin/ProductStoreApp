import { SearchByNameFilterPipe } from './search-by-name-filter.pipe';

describe('SearchByNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
