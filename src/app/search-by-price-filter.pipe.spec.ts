import { SearchByPriceFilterPipe } from './search-by-price-filter.pipe';

describe('SearchByPriceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByPriceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
